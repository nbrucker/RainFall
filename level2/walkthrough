les étapes afin de réussir : Comprendre le binaire donné
- On test le programme level2
- taille du buffer => 76 (taille) + 8 (taille return)
- On a crée une command 1 contenant la taille exact avec un shellcode (command 1)
- execute la commande de script dans GDB (r < test)
- nous voici dans un shell
- il nous suffit de voir le mot de passe dans le home de level3
  - on execute la derniere commande
